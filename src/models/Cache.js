class Cache {
    geolocalizationDataList = [];
    exchangeRates = {};

    // getExchangeRate(date) {
    //     return this.exchangeRates.find(r => r.date == date);
    // }
}

module.exports = Cache;